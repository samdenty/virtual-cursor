import { CursorIcon } from '../Cursor'
import { css } from '../utils'

export const MacOS: CursorIcon = {
  url: require('./assets/MacOS.png'),
  size: 18,
  style: css`
    filter: drop-shadow(1px 1px 1px rgba(0, 0, 0, 0.5));
  `
}
